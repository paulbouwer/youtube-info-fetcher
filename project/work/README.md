# Work Packages

This folder contains work packages — the primary unit of planned work in this project.

Each work package is a subfolder named `YYYYMMDD-short-description` containing a specification, plan, and verification results.

**→ See `$AGENT_SYSTEM_FOLDER/standards/project/work-package.md` for the work package standard.**
